from __future__ import annotations


class HouseStatus:
    DRAFT = "draft"
    LISTED = "listed"
    OFFLINE = "offline"


class AppointmentStatus:
    PENDING = "pending"
    CONFIRMED = "confirmed"
    REJECTED = "rejected"
    CANCELLED = "cancelled"
    EXPIRED = "expired"
